# user-service

Microservicio responsable de la gestión de usuarios (registro, autenticación, roles).

Arranque rápido:
- Requisitos: JDK 17, Maven
- Ejecutar: mvn spring-boot:run

Endpoints básicos a implementar:
- POST /api/auth/register
- POST /api/auth/login
- GET /api/users/{id}

